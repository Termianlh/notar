# Notar

> AI agent 的中立身份与信誉层。

## Quickstart

<!-- TODO: 填写安装和使用步骤 -->

## 项目结构

- `sdk/notar/` — 开源 SDK（身份生成、签名、验证）
- `registry/`  — 托管注册中心（FastAPI + SQLite/Postgres）
- `examples/`  — demo
- `tests/`     — 测试
